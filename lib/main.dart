import 'package:flutter/material.dart';
import 'package:flutter_bot/screens/getStarted.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return GetStarted();
  }
}